<Window x:Class="WeightBridgeApp.GatePassLookupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gate Pass Lookup" Height="420" Width="720"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="{x:Type DataGrid}">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="EnableRowVirtualization" Value="True" />
            <Setter Property="EnableColumnVirtualization" Value="True" />
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Open Gate Pass Lookup" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10" />

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Gate Pass Number" FontWeight="SemiBold" />
                <TextBox x:Name="GatePassFilterTextBox" TextChanged="FilterTextBox_TextChanged" Height="28" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Vehicle Plate" FontWeight="SemiBold" />
                <TextBox x:Name="VehicleFilterTextBox" TextChanged="FilterTextBox_TextChanged" Height="28" />
            </StackPanel>
            <StackPanel Grid.Column="4">
                <TextBlock Text="Party" FontWeight="SemiBold" />
                <TextBox x:Name="PartyFilterTextBox" TextChanged="FilterTextBox_TextChanged" Height="28" />
            </StackPanel>
        </Grid>

        <DataGrid x:Name="LookupDataGrid" Grid.Row="2" ItemsSource="{Binding LookupRows}" SelectedItem="{Binding SelectedGatePass, Mode=TwoWay}" MouseDoubleClick="LookupDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Gate Pass Number" Binding="{Binding GatePassNumber}" Width="160" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="90" />
                <DataGridTextColumn Header="Vehicle Plate" Binding="{Binding VehiclePlate}" Width="120" />
                <DataGridTextColumn Header="Driver" Binding="{Binding DriverName}" Width="130" />
                <DataGridTextColumn Header="Party Type" Binding="{Binding PartyType}" Width="90" />
                <DataGridTextColumn Header="Party Account" Binding="{Binding PartyAccount}" Width="120" />
                <DataGridTextColumn Header="Party Name" Binding="{Binding PartyName}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="3" Margin="0,10,0,0" LastChildFill="False">
            <TextBlock x:Name="StatusTextBlock" DockPanel.Dock="Left" VerticalAlignment="Center" Foreground="#202A44" />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="Select" Width="90" Margin="0,0,8,0" Click="SelectButton_Click" />
                <Button Content="Cancel" Width="90" Click="CancelButton_Click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
